/**
 * Shared constants for the Navigation Library.
 */

// Default route for fallback mechanisms
export const DEFAULT_ROUTE = '/';

// Animation duration for sidebar transitions (matches LESS variables)
export const SIDEBAR_TRANSITION_MS = 200;

// LocalStorage key for persisting sidebar state (if needed later)
export const SIDEBAR_COLLAPSED_KEY = 'enterprise_sidebar_collapsed';
