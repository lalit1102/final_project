export const DEFAULT_MODAL_WIDTH = 520;
export const DEFAULT_CONFIRM_OK_TEXT = "OK" as const;
export const DEFAULT_CONFIRM_CANCEL_TEXT = "Cancel" as const;
export const DEFAULT_MODAL_OK_TEXT = "Submit" as const;
